<section class="section hero">
	<div class="container">
		<div class="hero_content">
			<h1>Sites</h1>

			<p>
				I design and build <span>mobile-responsive websites</span> that are
				{" "}<span>fast</span>, <span>optimized</span> for SEO, and <span
					>convert</span
				> visitors into customers. Whether you're launching a new site or
				revamping an existing one, I ensure it looks great and performs well
				across all devices. I work directly with business and freelancers,
				as well as with agencies as an outside contractor.
			</p>
			<a href="/contact" class="btn btn-primary hover-alt">Get Started</a>
		</div>
	</div>
</section>

<style>
	.hero {
		position: relative;
		background-color: var(--cream);
	}

	.hero .container {
		padding-block: 10.5rem 5.75rem;
		max-width: 64rem;
	}

	.hero h1 {
		font-size: 4rem;
		color: var(--red);
		margin-bottom: 1.5rem;
		display: flex;
		align-items: center;
		gap: 0.5rem;
		flex-wrap: wrap;
	}

	.hero p {
		font-size: 1rem;
		color: rgb(from var(--brown) r g b / 0.75);
		text-wrap: pretty;
		margin-bottom: 2.5rem;
	}

	.hero p span {
		opacity: 1;
		background: var(--red);
		color: var(--cream);
	}

	@media screen and (max-width: 767px) {
		.hero h1 {
			font-size: 2.5rem;
		}

		.hero p {
			max-width: 100%;
		}
	}
</style>
