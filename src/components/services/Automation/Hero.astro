<section class="section hero">
	<div class="container">
		<div class="hero-grid">
			<div class="top">
				<h1 class="reveal reveal-on-load">Automation</h1>
			</div>
			<div>
				<p class="reveal reveal-on-load">
					I architect and develop automation workflows that{" "}
					<span
						>take the repetitive, manual work off your team's plate.</span
					>{" "}
					Data entry, syncing between tools, social media content, invoicing,
					reporting — the tasks that quietly eat hours every week get handled
					in the background, reliably, without anyone thinking about them.
					I map how your business actually runs, connect the systems you
					already use, and build the logic that ties them together. The
					result is fewer dropped balls, fewer mistakes, and team members
					that can{" "}
					<span>put their energy toward what they are best at.</span>
				</p>
				<div class="hero-cta reveal reveal-on-load">
					<a
						href="#details"
						class="btn btn-outline-secondary
				reveal
				reveal-on-load"
						>Learn More</a
					>
					<a
						href="/contact"
						class="btn btn-secondary hover-alt
				reveal
				reveal-on-load"
						>Get Started</a
					>
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	.hero {
		position: relative;
		background-color: var(--cream);
	}

	.hero .container {
		padding-block: 8rem 5rem;
		max-width: 64rem;
	}

	.hero h1 {
		font-size: 4rem;
		color: var(--red);
		margin-bottom: 1.5rem;
		display: flex;
		align-items: center;
		gap: 0.5rem;
		flex-wrap: wrap;
	}

	.hero p {
		font-size: 1rem;
		color: rgb(from var(--brown) r g b / 0.75);
		text-wrap: pretty;
		margin-bottom: 2.5rem;
	}

	.hero p span {
		opacity: 1;
		background: var(--red);
		color: var(--cream);
	}

	.hero-cta {
		display: flex;
		gap: 0.875rem;
		flex-wrap: wrap;
		align-items: center;
	}

	@media screen and (max-width: 767px) {
		.hero h1 {
			font-size: 2.5rem;
		}

		.hero p {
			max-width: 100%;
		}
	}
</style>
