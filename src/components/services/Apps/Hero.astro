<section class="section hero">
	<div class="container">
		<div class="hero_content">
			<h1 class="reveal reveal-on-load">Apps</h1>
			<p class="reveal reveal-on-load">
				I design and build <span>web and mobile apps</span> end to end — from
				the first sketch to a product in your users' hands. Whether it's a
				customer-facing platform, an internal tool, or an idea you've been
				sitting on, I handle the whole stack: intuitive interfaces, reliable
				backends, databases, and the integrations that tie it all together.
				Everything is built to be fast, maintainable, and ready to grow with
				you — so you end up with <span
					>software people enjoy using.</span
				>
			</p>
			<div class="hero-cta reveal reveal-on-load">
				<a
					href="#details"
					class="btn btn-outline-secondary
				reveal
				reveal-on-load"
					>Learn More</a
				>
				<a
					href="/contact"
					class="btn btn-secondary hover-alt
				reveal
				reveal-on-load"
					>Get Started</a
				>
			</div>
		</div>
	</div>
</section>

<style>
	.hero {
		position: relative;
		background-color: var(--cream);
	}

	.hero .container {
		padding-block: 8rem 5rem;
		max-width: 64rem;
	}

	.hero h1 {
		font-size: 4rem;
		color: var(--red);
		margin-bottom: 1.5rem;
		display: flex;
		align-items: center;
		gap: 0.5rem;
		flex-wrap: wrap;
	}

	.hero p {
		font-size: 1rem;
		color: rgb(from var(--brown) r g b / 0.75);
		text-wrap: pretty;
		margin-bottom: 2.5rem;
	}

	.hero p span {
		opacity: 1;
		background: var(--red);
		color: var(--cream);
	}

	.hero-cta {
		display: flex;
		gap: 0.875rem;
		flex-wrap: wrap;
		align-items: center;
	}

	@media screen and (max-width: 767px) {
		.hero h1 {
			font-size: 2.5rem;
		}

		.hero p {
			max-width: 100%;
		}
	}
</style>
